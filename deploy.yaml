apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-java-webapp
  namespace: default
spec:
  replicas: 7
  selector:
    matchLabels:
      app: my-java-webapp
  template:
    metadata:
      labels:
        app: my-java-webapp
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: my-java-webapp
          image: docker-repo.sk-devops.com/puneeth_nexus_push/my-java-webapp:10db503
          ports:
            - containerPort: 8080
